
# Validation Report Executive Summary
***

This Validation Report contains the results of the Validation
Process executed over the Netapp {{version}}

The repo used for Validation has been: {{repo}}
The branch used for Validation has been: {{branch}}
The last commit ID has been: {{commit}}
The environment used has been: {{environment}}
The build number at Jenkins has been: {{build_number}}
Total validation time has been: **{{total_duration}}**

The result of the Validation Process over the Netapp {{version}} has been: 
{%- if result == "SUCCESS" %}
**<span style="color:green">{{result}}</span>**
{%- else %}
**<span style="color:red">{{result}}</span>**
{% endif %}

The individual result of the validations test has been as show in the following table:

{% set vars = {'foo': False} %}
{% set counter = 0 -%}
{% for step_name,step_status in steps.items() %}
  STEP {{ counter + loop.index0 }}: {{ step_name|upper|replace("-"," ") }} status: {{step_status}}
    {%- if step_status == "FAILURE" %}
      {% if vars.update({'foo': True}) %} 
      {% endif %}
    {%- endif %}
  {% set counter = counter + group|length %}
{% endfor -%}
{% if vars.foo %}
  **Please, take a look to those steps that are failing.**
{% endif %}


In the following pages, we provide details of the tests executed 
and the results.







